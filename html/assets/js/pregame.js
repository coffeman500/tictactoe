// IO Variable, yay!
// Connect to the pregame namespace
var socket = io('http://localhost:3000/pregame');

// TODO: Verify user's token against both his username and make sure his auth's active match matches one that is actually active
